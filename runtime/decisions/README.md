# Approval Buckets

Runtime approval records are created here by the control plane.

Tracked structure:

- `pending/`
- `approved/`
- `rejected/`
- `expired/`

All JSON approval payloads inside these runtime buckets are ignored by git.
