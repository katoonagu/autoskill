job_name: feedback_validation

browser_profile:
  provider: adspower
  profile_no: "333"

state:
  state_file: automation/state/feedback_validation_state.json

outputs:
  base_dir: output/feedback_validation
  validation_md: output/feedback_validation/validation.md

memory:
  wiki_root: knowledge/llm_wiki

policy:
  human_approval_required: true
  can_contact_reviewers: false
  can_contact_commenters: false
