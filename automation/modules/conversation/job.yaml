job_name: conversation

browser_profile:
  provider: adspower
  profile_no: "337"

state:
  state_file: runtime/state/conversation_state.json

outputs:
  base_dir: artifacts/conversation
  threads_md: artifacts/conversation/threads.md

memory:
  wiki_root: knowledge/llm_wiki

policy:
  human_approval_required: true
  rate_limit_per_day: 20
  allowed_channels:
    - telegram
    - email
    - instagram_dm

